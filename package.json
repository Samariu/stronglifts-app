{
  "name": "stronglifts-app",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "backend": "cd backend && node server.js",
    "backend:dev": "cd backend && node --watch server.js"
  }
}
